#include "LicensedVirtualCableBackend.h"

// All of LicensedVirtualCableBackend is implemented inline in the header --
// there is no real driver integration to compile against yet (see the class
// doc comment for why). This translation unit exists so the build graph
// matches the documented file layout.
